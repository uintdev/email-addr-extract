[package]
name = "emailaddrextract"
version = "1.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
regex = "1.13.1"

[profile.release]
opt-level = 3
panic = "abort"
debug = false
strip = "symbols"
lto = true
